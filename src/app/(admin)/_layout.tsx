import { Stack } from 'expo-router';

/**
 * Layout for all authenticated admin screens.
 * Phase 0 placeholder: the DrawerNavigator/AdminShell and the
 * authentication guard are built out in Phase 3 — Navigation shell.
 */
export default function AdminLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
