import { Text, View } from 'react-native';

/** Placeholder. Built out in Phase 10 — Markets. */
export default function MarketsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Markets — Phase 10</Text>
    </View>
  );
}
