import { Text, View } from 'react-native';

/** Placeholder. Built out in Phase 7 — Orders. */
export default function OrdersScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Orders — Phase 7</Text>
    </View>
  );
}
