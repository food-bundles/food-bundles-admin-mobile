import { Text, View } from 'react-native';

/** Placeholder. Built out in Phase 9 — Stock. */
export default function ProductsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Products — Phase 9</Text>
    </View>
  );
}
