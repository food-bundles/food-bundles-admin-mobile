import { Text, View } from 'react-native';

/** Placeholder. Built out in Phase 8 — Users. */
export default function AffiliatorsScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Affiliators — Phase 8</Text>
    </View>
  );
}
