import { Text, View } from 'react-native';

/** Placeholder login screen. Built out in Phase 5 — Auth. */
export default function Login() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Login — Phase 5</Text>
    </View>
  );
}
